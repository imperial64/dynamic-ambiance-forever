# In-game feedback on the 70009 persistence revision (2026-09-25)

Recorded verbatim as given, during the in-game check of the uncommitted revision (build
1.60.1.70009). Screenshots were shown in the session, not stored.

1. Delete zone ran off the panel: "delete button is off screen, put the delete under revert
   button". Done in the session: Delete zone now sits under Revert.
2. "add the export import buttons to the bottom right of the panel. import button was
   completely missing."
3. "revert button text is too long make it just say revert to last export, tooltip
   description of the button should read "Last export: version + date + time like how it is
   now."
4. "export panel should not be in the same area as import panel."
5. "make the export already copy the string into clipboard if possible. if not add a little
   instruction under the box that says, Select all -> ctrl C"
6. "import doesnt have to specify old DA1 format, no one used it yet."
